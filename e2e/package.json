{
  "name": "ccdexplorer-site-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright UI tests for the ccdexplorer_site project.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
